<div class="home-banner">
    <div class="container">
      <div class="row text-center">
        <div class="col-md-12 p-2">
          <h1 class="display-2 text-white">Online Journalism</h1>
          <h3 class="text-white">Writing blogs & content that matter</h3>
          <% if(!loggedIn) { %>
            <a href="/register" class="btn btn-primary">Join Us</a> 
            <a href="/about" class="btn btn-light">About Us</a>
          <% } %>
          <% if(loggedIn) { %>
            <a href="/logout" class="btn btn-primary">Logout</a> 
            <a href="/blogs" class="btn btn-light">Read Blogs</a>
          <% } %>
        </div>
      </div>
    </div>
  </div>
